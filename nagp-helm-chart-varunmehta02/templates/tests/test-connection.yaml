apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nagp-helm-chart-varunmehta02.fullname" . }}-test-connection"
  labels:
    {{- include "nagp-helm-chart-varunmehta02.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nagp-helm-chart-varunmehta02.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
