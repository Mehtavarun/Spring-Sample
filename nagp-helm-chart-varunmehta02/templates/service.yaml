apiVersion: v1
kind: Service
metadata:
  name: {{ include "nagp-helm-chart-varunmehta02.fullname" . }}
  labels:
    {{- include "nagp-helm-chart-varunmehta02.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nagp-helm-chart-varunmehta02.selectorLabels" . | nindent 4 }}
